# SyncSpace Environment Configuration Template

# Client Configuration:
# Point to your deployed WebSocket backend (e.g., on Render, Railway, or Fly.io)
# For local development, leave blank to default to ws://localhost:8080
VITE_WS_URL=ws://localhost:8080

# Server Configuration:
PORT=8080
HOST=0.0.0.0
